Instrctions to Run the Assignment
1) Load both the AppealsServer and AppealsClient in NetBeans
2) Run the AppealsServer. When the index page is loaded run the AppealsClient project.
   The main method is inside the class Main.java. The output is printed in the console.
3) The project report containing the representation format, uri end points are included in this folder.